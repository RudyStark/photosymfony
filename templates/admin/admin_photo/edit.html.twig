{% extends 'admin/admin_base.html.twig' %}

{% block title %}Edit Photo{% endblock %}

{% block admin_content %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <h1>Edit Photo</h1>

                {{ include('admin/admin_photo/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_admin_photo_index') }}" class="btn btn-primary btn-sm"><i class="fas fa-arrow-left"></i> Back to list</a>

                {{ include('admin/admin_photo/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
